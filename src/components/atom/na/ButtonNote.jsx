import React, { Component } from "react";

export class ButtonNote extends Component {
  render() {
    const link = this.props.link;
    const buttonName = this.props.button;
    return (
      <button>
        buttonName
      </button>
    );
  }
}

export default ButtonNote;
